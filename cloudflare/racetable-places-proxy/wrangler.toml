name = "racetable-places-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# KV namespace for caching
[[kv_namespaces]]
binding = "PLACES_CACHE"
id = "TODO_ADD_KV_NAMESPACE_ID"

# secrets (set via CLI):
# npx wrangler secret put GOOGLE_PLACES_API_KEY

# Note: KV namespace must be created first:
# npx wrangler kv:namespace create "PLACES_CACHE"
# Then copy the id above
